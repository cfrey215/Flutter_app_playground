on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    env:
      OUTPUT_NAME: \"chrisfrey\"
    steps:
      # To use this repository's private action,
      # you must check out the repository
      - name: Checkout
        uses: actions/checkout@v4

      - name: Hello world action step
        uses: actions/action@main  # Uses an action in the root directory
        id: hello
        with:
          who-to-greet: 'Dr Drizzay!'

    #   - name: 'Hello World'
    #     id: hello
    #     run: |
    #         using: 'docker'
    #         image: 'Dockerfile'
    #         args:
    #           - 'Dr. Dre, homey!'
    #     outputs:
    #         time: # id of output

      # - name: Build docker images
      #   run: docker build -t flutter_playground_image .
    
      # - name: Run tests
      #   id: tests2
      #   run: docker run flutter_playground_image ${{ env.OUTPUT_NAME }}

      # Use the output from the `hello` step
      - name: Get the output time
        run: echo "The time was ${{ steps.hello.outputs.time }}"
