name: flutter_app_playground
description: A new Flutter project.

# publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.19.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0

dev_dependencies:
  test: any
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  patrol: 2.3.0

flutter:
  uses-material-design: true

patrol:
  app_name: flutter_app_playground
  android:
    package_name: com.example.flutter_app_playground
  ios:
    bundle_id: com.example.flutter_app_playground